# DevAI Pro — AI Powered Application Builder Platform

Enterprise-grade MERN SaaS to generate, manage, and export AI-built applications.

This repository contains two top-level apps:
- `server` — Node.js + Express backend (TypeScript)
- `client` — React + Vite frontend (TypeScript)

See server and client folders for setup and deployment instructions.
